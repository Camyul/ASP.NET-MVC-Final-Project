@using TelerikAcademy.FinalProject.Web.Models.Home
@model ICollection<ProductViewModel>

@{
    ViewBag.Title = "Products Page";
}

@*<div id="search">
    @using (@Html.BeginForm()) 
    {
        <input class="form-control-inline" type="text" name="searchName" />
        <input class="form-control-inline btn btn-info" type="submit" value="Search" name="search"/>
    }
</div>*@
<div id="product-container">
    <div id="categories">
        <p>Listofcategories</p>
    </div>
    <div id="products">
        <div class="row">
            <div class="latest-recipes row eq-height">
                <h3 class="col-sm-12" id="enjoy">All Products</h3>
                <div class="col-sm-12 eq-height">
                    @foreach (var product in Model)
                    {
                        @Html.Partial("_ProductPreviewPartial", product)
                    }
                </div>
            </div>
        </div>
    </div>
</div>